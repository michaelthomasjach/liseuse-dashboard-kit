node_modules
dist
storybook-static
*.local
.DS_Store
npm-debug.log*
debug-storybook.log

# Never commit a real GitHub Packages token
.npmrc.local

# Standalone app build output (see vite.config.app.ts) — published by CI, never committed.
app-dist
